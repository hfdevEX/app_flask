name: Dockle Security Scan

on:
  push:
    branches:
      - dockle_scan_test
  pull_request:
    branches:
      - main

jobs:
  dockle-scan:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu- 22.04.2]

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Docker
        uses: docker/setup-buildx-action@v1

      - name: Run Dockle
        run: |
          docker run --rm -v ${{ github.workspace }}:/workspace goodwithtech/dockle:latest /workspace
